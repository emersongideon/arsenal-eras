# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ipynb_checkpoints/

# Node
node_modules/
web/dist/

# Cached raw data.
#  - StatsBomb events/lineups (~106 MB) are regenerated by build_dataset scripts.
#  - The small Understat cache IS committed so the pipeline runs without scraping.
data/raw/events_*.json
data/raw/lineup_*.json
data/raw/matches_*.json

# OS / editor
.DS_Store
.vscode/
.idea/

# NOTE: data/processed/*.json and web/public/data/*.json ARE committed on purpose
# (~0.5 MB total) so the app + notebook run fully offline with no build-time
# downloads. Regenerate anytime with `python -m analysis.build`.
